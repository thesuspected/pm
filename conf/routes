# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
GET     /tasks                                  App.Tasks
GET     /employees                              App.Employees


GET     /project                                CProject.GetAll
GET     /project/:id                            CProject.Get
GET     /projectFk/:id                          CProject.GetFk
PUT     /project                                CProject.Create
POST    /project/:id                            CProject.Update
DELETE  /project/:id                            CProject.Delete


GET     /group                                  CGroup.GetAll
GET     /group/:id                              CGroup.Get
GET     /groupFk/:id                            CGroup.GetFk
PUT     /group                                  CGroup.Create
POST    /group/:id                              CGroup.Update
DELETE  /group/:id                              CGroup.Delete


GET     /employee                               CEmployee.GetAll
GET     /employee/:id                           CEmployee.Get
GET     /employeeFk/:id                         CEmployee.GetFk
GET     /employeeByGroup/:id                    CEmployee.GetByGroup
GET     /employeeByGroupFk/:id                  CEmployee.GetByGroupFk
# PUT     /employee                             CEmployee.Create
# POST    /employee/:id                         CEmployee.Update
# DELETE  /employee/:id                         CEmployee.Delete


# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action